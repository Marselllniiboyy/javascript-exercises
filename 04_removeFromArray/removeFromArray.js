const removeFromArray = function(a,b,c,d,e) {
    // const filterArr = a.filter( number=> number !== b && number !== c && number !== d && number !== e );
    // return filterArr;
    const filterArr =array.filter(number => !args.includes(number))
    return filterArr 
};

// Do not edit below this line
module.exports = removeFromArray;
